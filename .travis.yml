before_script:
  - npm install -g elm
script: elm-make Main.elm --output=index.html --yes
deploy:
  provider: pages
  skip-cleanup: true
  github-token: $GITHUB_TOKEN  # Set in travis-ci.org dashboard, marked secure
  keep-history: true
  on:
    branch: master